language: node_js
node_js:
  - 12.18.2
  
install:
  - npm ci
  
script: 
  - npm run generate
  - npm run deploy
  
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local_dir: build
  on: 
    branch: gh-pages
    
