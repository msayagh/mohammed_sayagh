language: node_js
node_js:
  - 12.18.2
  
install:
  - npm install
  
script: 
  - npm run generate
  
deploy:
  provider: pages
  skip-cleanup: true
  strategy: git
  token: $GITHUB_TOKEN
  local_dir: build
  target-branch: gh-pages
  keep_history: true
  on: 
    branch: main
    
